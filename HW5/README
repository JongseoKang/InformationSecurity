ID: 2020147555
Name: Jongseo Kang

Part A
elf_parser.h 는 elf file 의 실행 가능한 section의 코드를 전부 concat 하는 함수인 parseExecScn() 함수와
.signature section의 코드를 잘라내는 parseSignScn() 함수를 포함하고 있다. parseSignScn() 함수의 경우
.signature section이 존재하지 않는 경우 retFlag 를 통해 sign 되지 않음을 반환한다.
rsa_sign.h 는 rsa sign 과 rsa verify 를 위한 함수가 존재한다. 사용된 서명방식은 nid_sha256 을 이용하였다.

Part B
코드상에서 구현되어 있는 do_nothing() 함수를 이용하였다. assign 된 전역변수로 설정되어있는 license_check 함수 pointer 의
초기값을 기존 do_license_check_v2() 에서 do_nothing()으로 바꿔주었다. 또한 .rela.dyn section의 값도 동일하게
변경해주어 relocation 이 되어도 do_nothing()함수를 가리키도록 하였다.